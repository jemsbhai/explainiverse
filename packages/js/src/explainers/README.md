# Explainers placeholder

This directory is intentionally empty. The experimental JavaScript workspace
does **not** implement, port, or export any Explainiverse explainer algorithm.
Names shown by the demo are synthetic labels only and must not be interpreted as
method availability or paper equivalence.

Any future port requires an independent formula, target, output-space, and
failure-semantics audit before it can be exported.
