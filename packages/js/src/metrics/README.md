# Metrics placeholder

This directory is intentionally empty. The experimental JavaScript workspace
does **not** implement, port, or export any Explainiverse evaluation metric.

Any future metric requires an independent equation, aggregation, score-space,
edge-case, and reference audit before it can be exported.
